{
  "name": "surucu-akademisi-functions-institution-chat",
  "description": "Isolated institution province chat Cloud Functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"no lint configured\"",
    "build": "echo \"no build step (plain JS)\"",
    "serve": "firebase emulators:start --only functions:institutionChat",
    "deploy": "firebase deploy --only functions:institutionChat",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5"
  }
}
