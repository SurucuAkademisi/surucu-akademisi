{
  "name": "surucu-akademisi-functions-payments",
  "description": "Payment infrastructure for individual video lessons premium (skeleton / stubs)",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"no lint configured\"",
    "build": "echo \"no build step (plain JS)\"",
    "serve": "firebase emulators:start --only functions:payments",
    "deploy": "firebase deploy --only functions:payments",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "6.4.0"
  }
}
