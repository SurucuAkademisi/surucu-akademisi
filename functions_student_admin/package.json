{
  "name": "surucu-akademisi-functions-student-admin",
  "description": "Dedicated callable functions for tenant student admin operations",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"no lint configured\"",
    "build": "echo \"no build step (plain JS)\"",
    "serve": "firebase emulators:start --only functions:student-admin",
    "deploy": "firebase deploy --only functions:student-admin",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "6.4.0"
  }
}
